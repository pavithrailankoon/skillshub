package com.example.skillshub;

import android.content.Intent;
import android.os.Bundle;

import androidx.activity.EdgeToEdge;
import androidx.appcompat.app.AppCompatActivity;
import androidx.core.graphics.Insets;
import androidx.core.view.ViewCompat;
import androidx.core.view.WindowInsetsCompat;

import com.example.skillshub.signupform.RegistrationControlActivity;

public class RegistrationActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {

        super.onCreate(savedInstanceState);

//        clientButton.setOnClickListener(v -> {
//            // Redirect to RegistrationActivity with client registration type
//            redirectToRegistration("client");
//        });
//
//        workerButton.setOnClickListener(v -> {
//            // Redirect to RegistrationActivity with worker registration type
//            redirectToRegistration("worker");
//        });
//
//        private void redirectToRegistration(String registrationType) {
//            Intent intent = new Intent(CategorizationActivity.this, RegistrationControlActivity.class);
//            intent.putExtra("registrationType", registrationType);
//            startActivity(intent);
//        }
    }
}